package Representation;
import java.util.Scanner;


public abstract class DecisionNode extends InnerNode{
/*
    @Override
    public Node choseNext(){
        Scanner scanner = new Scanner (System.in);
        int choix;
        System.out.println("Que choisis-tu entre les 4 options ?");

        try {
             choix = scanner.nextInt();
        } catch (Exception e) {
            System.out.println("Erreur : Que choisis-tu entre les 4 options ");
            choix = scanner.nextInt();
        }

        // Utiliser la valeur entrée par l'utilisateur (si aucune exception n'a été levée)
        System.out.println("Vous avez entré : " + choix);
    }
*/
}
